# ClawX keeper — settles expired prediction rounds on Avalanche Fuji
FROM node:20-alpine
WORKDIR /app
ENV NODE_ENV=production
COPY package.json package-lock.json ./
RUN npm ci --omit=dev
COPY scripts ./scripts
COPY utils ./utils
CMD ["node", "scripts/keeper.js"]
